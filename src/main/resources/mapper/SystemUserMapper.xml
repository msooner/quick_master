<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ron.mapper.SystemUserMapper">
    <select id="getUser" resultType="com.ron.entity.SystemUser">
        select * from r_system_user where user_id = #{user_id}
    </select>
    
    <select id="getLoginUser" resultType="com.ron.entity.SystemUser">
        select id, username, role_names, role_ids from r_system_user where username = #{username} and password = #{password}
    </select>
</mapper>