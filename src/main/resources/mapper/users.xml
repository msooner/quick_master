<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ron.mapper.SystemUsersMapper">
    <select id="getUser" resultType="com.ron.entity.SystemUser">
        select userId, email, userName, nickName, avatar, sex, birthday, mobilePhone, consumptionCount, goodsComment,
        addressId, groupId, updateTime, isFirstOrder, balance, integral from r_users where user_id = #{user_id}
    </select>
</mapper>